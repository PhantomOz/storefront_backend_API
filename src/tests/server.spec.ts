describe("it should be able to multiply", () => {
  it("multiply two numbers", () => {
    const x = 2 * 2;
    expect(x).toEqual(4);
  });
  it("Multiply 3 numbers", () => {
    const x = 3 * 2 * 4;
    expect(x).toEqual(24);
  });
});
